// WiFi credentials for OTA updates.
// Copy this file to secrets.h, fill in your details, then keep secrets.h local.
// secrets.h is gitignored and will never be committed.
#pragma once
#define WIFI_SSID  "your-network-name"
#define WIFI_PASS  "your-password"
// OTA password (prompted by Arduino IDE / arduino-cli during upload):
#define OTA_PASS   "lathe"
